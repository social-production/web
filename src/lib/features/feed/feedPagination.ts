/** @deprecated Import from `$lib/types/pagination` — kept for gradual migration. */
export {
  DEFAULT_FEED_PAGE_SIZE,
  appendUniqueById,
  toFeedPageResult,
  type FeedPageResult
} from '$lib/types/pagination';
